extends layout

block content
	style(src="css/style.css")
	h1= title
	p Welcome to #{title}
	#ajaxreload
	#logs
		p!= msg
	script(type='text/javascript').
		setTimeout("location.reload()",1000*3);
	//script(src="ajax.js")